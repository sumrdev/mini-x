version: '3.8'


services:
  minix:
    image: minixgroupd/miniximage:latest
    deploy:
      replicas: 3
      update_config:
          parallelism: 3
    env_file:
      - .env
    ports:
      - "5000:5000"
      - "5001:5001"
    
