version: '3.3'


services:
  minix:
    image: minixgroupd/miniximage:latest
    deploy:
      replicas: 2
    ports:
      - "5000:5000"
      - "5001:5001"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    
